from fastapi import APIRouter

# Import endpoint routers here when they're created
# from app.api.v1.endpoints import elements, recipes, crafting

api_router = APIRouter()

# Include endpoint routers here when they're created
# api_router.include_router(elements.router, prefix="/elements", tags=["elements"])
# api_router.include_router(recipes.router, prefix="/recipes", tags=["recipes"])
# api_router.include_router(crafting.router, prefix="/craft", tags=["crafting"])
